import { CurrencyTndPipe } from './currency-tnd.pipe';

describe('CurrencyTndPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyTndPipe();
    expect(pipe).toBeTruthy();
  });
});
